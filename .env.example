# PORT: Optional server port. Defaults to 3000.
PORT=3000

# BROWSER_EXTENSION_PATHS: Optional Chrome extension folders.
# Separate multiple unpacked extension paths with ";" on Windows or ":" on Linux/macOS.
BROWSER_EXTENSION_PATHS=""
